namespace Ikariam
{
    public class TopUser
    {
        public string AllyName, AllyId, PlayerId;

        public TopUser(string AllyName, string AllyId, string PlayerId)
        {
            this.AllyName = AllyName;
            this.AllyId = AllyId;
            this.PlayerId = PlayerId;
        }
    }
}
